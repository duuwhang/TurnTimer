trigger:
  - manual


steps:
  - checkout: self
    persistCredentials: true
    clean: true
    displayName: Checkout GitHub Repository

  - script: echo "##vso[task.setvariable variable=Version]`grep 'versionName' $(Root)app/build.gradle | tr '"' ' ' | tr 'versionName' ' '`"
            echo 'Version = $(Version)'
    displayName: Read Release App Version

  - task: Gradle@2
    inputs:
      workingDirectory: ''
      gradleWrapperFile: 'gradlew'
      gradleOptions: '-Xmx3072m'
      publishJUnitResults: false
      testResultsFiles: '**/TEST-*.xml'
      tasks: 'assembleRelease'

  - task: AndroidSigning@2
    inputs:
      apkFiles: '$(Root)app/build/outputs/apk/release/*.apk'
      jarsign: true
      jarsignerKeystoreFile: 'duu_whang_keystore.keystore'
      jarsignerKeystorePassword: '$(Release.Keystore.Password)'
      jarsignerKeystoreAlias: 'duu_whang_release_key'
      jarsignerKeyPassword: '$(Release.Key.Password)'
      jarsignerArguments: '-verbose -sigalg SHA256withECDSA'
      zipalign: true

  - task: CopyFiles@2
    inputs:
      sourceFolder: '$(Root)app/build/outputs/apk/release/'
      Contents: '*.apk'
      TargetFolder: '$(Root)'
      OverWrite: true

  - script: git config user.email "maxipulstorm@aon.at";
            git config user.name "duuwhang";
            git checkout master;
            git add .;
            git commit -m "CD commit";
            git push origin;

  - task: GitHubRelease@0
    displayName: Create GitHub Release
    inputs:
      gitHubConnection: github.com_duuwhang
      repositoryName: TurnTimer
      action: create
      target: '$(Version)' # Required when action == Create || Action == Edit
      tagSource: manual
      tagPattern: 'User specified tag' # Optional
      tag: '$(Version)' # Required when action == Edit || Action == Delete || TagSource == Manual
      #title: # Optional
      #releaseNotesSource: 'file' # Optional. Options: file, input
      #releaseNotesFile: # Optional
      #releaseNotes: # Optional
      #assets: '$(Build.ArtifactStagingDirectory)/*' # Optional
      #assetUploadMode: 'delete' # Optional. Options: delete, replace
      #isDraft: false # Optional
      #isPreRelease: false # Optional
      #addChangeLog: true # Optional
      #compareWith: 'lastFullRelease' # Required when addChangeLog == True. Options: lastFullRelease, lastRelease, lastReleaseByTag
      #releaseTag: # Required when compareWith == LastReleaseByTag
